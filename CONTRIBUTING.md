# المساهمة

شكراً لمساهمتك. المشروع Monorepo: خدمات Python داخل `apps/`، مكتبات مشتركة داخل
`libs/`، وواجهة Vinext/Next في الجذر.

## بدء العمل

1. أنشئ فرعاً من `main`.
2. نفّذ `make setup` وحدّث `.env` بقيم محلية فقط.
3. شغّل `make dev`.
4. أضف اختبارات للتغيير، ثم شغّل `make check`.
5. افتح Pull Request صغيراً يشرح السلوك، المخاطر، وخطة التراجع.

لا تلتزم بأسرار أو ملفات `.env` أو ملفات وسائط ناتجة. استخدم migrations بدلاً من
تعديل قاعدة بيانات مشتركة يدوياً. التغييرات الحساسة (المصادقة، التشفير، صلاحيات
الأدوات، GitHub والنشر) تحتاج مراجعة أمنية ثانية.

## معايير الكود

- Python 3.12، Ruff، mypy، pytest، وواجهات async عند I/O.
- TypeScript strict، ESLint، ومكونات قابلة للوصول وتدعم RTL.
- Conventional Commits مفضلة: `feat:`, `fix:`, `docs:`, `test:`, `chore:`.
- لا تضف Adapter خارجي دون timeout، تنقيح أسرار، mapping للأخطاء واختبارات.

