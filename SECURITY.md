# سياسة الأمان

## الإبلاغ عن ثغرة

لا تنشئ Issue عاماً لتفاصيل ثغرة قابلة للاستغلال. استخدم GitHub Security
Advisories في المستودع، واذكر الأثر، خطوات إعادة الإنتاج، والإصدار المتأثر. لا
ترسل بيانات مستخدمين أو مفاتيح حقيقية.

## نموذج الأسرار

- مفاتيح AI وGitHub واتصالات قواعد البيانات تُشفّر في التطبيق قبل التخزين.
- `ENCRYPTION_MASTER_KEY` و`APP_SECRET_KEY` أسرار تشغيل ولا تدخل Git أو الصور.
- السجلات والتتبعات يجب أن تحجب Authorization وCookies وكلمات المرور.
- Base URLs المخصصة تخضع لحماية SSRF وتُمنع الشبكات الداخلية وmetadata افتراضياً.
- Production يتطلب HTTPS وSecure/HttpOnly cookies وWebhook secret قوياً.

دوّر أي سر ظهر في سجل أو Commit فوراً، ثم أزل أثره من السجل بالطريقة المعتمدة لدى
مسؤول المستودع. لا تعتمد على حذف الملف من آخر Commit فقط.

## العمليات عالية الخطورة

Push، Merge، نشر Production، migrations مدمرة، تعديل الأسرار، والوصول الكتابي
لقواعد البيانات تتطلب موافقة بشرية مسجلة. Sandbox الوكيل غير root وبحدود موارد
وشبكة، ولا يملك أسرار المنصة افتراضياً.

